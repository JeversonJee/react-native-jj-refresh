import { Component } from 'react';
import { requireNativeComponent } from 'react-native';

const SPModule = requireNativeComponent('RCTSpinnerModule');

const JJRefreshCtrl = requireNativeComponent('RNTRefreshView');

